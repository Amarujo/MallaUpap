<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gametest.www.mallaupap"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Malla Upap"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>
        <activity
            android:name=".FaltantesActivitie"
            android:label="Materias que me faltan">
        </activity>
        <activity
            android:name=".AprobadasActivity"
            android:label="Materias aprobadas">
            >
        </activity>
        <activity
            android:name=".CanceladasActivity"
            android:label="Materias canceladas">
        </activity>
        <activity android:name=".SearchResultActivity">
        </activity>
    </application>

</manifest>